node_modules/
dist/
build/
.output/
.netlify/
.tanstack/

# Real secrets — every app in this project has its own .env with live
# credentials (DB passwords, WhatsApp tokens, JWT secrets). Never commit these.
.env
.env.local
.env.*.local

*.log
npm-debug.log*

.DS_Store
Thumbs.db

*.tsbuildinfo

# Local working directories — raw asset dumps / one-off staging folders, not
# part of the deployed app (menu photos get applied straight to the server's
# uploads dir, not checked into the repo).
/Menu-images/
/menu-photo-stage6/
/qr-codes/
/frontend/src/assets/images/meny/
