# Copy to .env and adjust if needed. Never commit .env.

# Base URL of the separate Indish Loyalty app (../indish-loyalty).
# Used for the "View loyalty profile" / "Loyalty Dashboard" links in the admin panel.
VITE_LOYALTY_APP_URL=http://localhost:8080
